<%= form_for(@create_order) do |f| %>
  <% if @create_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@create_order.errors.count, "error") %> prohibited this create_order from being saved:</h2>

      <ul>
      <% @create_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
